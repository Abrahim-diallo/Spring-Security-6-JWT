package fr.springsecurityjwtapp.spring_jwt_app.enums;

public enum TokenType {
    /**
     * Jeton d'authentification de type "Bearer".
     * Jeton utilisé pour l'authentification par porteur (Bearer Authentication).
     */
    BEARER
}
